 providing advanced camera zoom controls with both discrete button and continuous wheel interfaces.

add to your `Package.swift`:

```swift
dependencies: [
    .package(url: "https://github.com/kvin-van/ZoomWheelLib", from: "1.0.0")
]
```


